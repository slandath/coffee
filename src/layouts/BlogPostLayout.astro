---
import MainLayout from "./MainLayout.astro";
const {frontmatter} = Astro.props
const { date, title} = frontmatter;
---

<MainLayout {title}>
    <article class="container py-3">
        <h1 class="text-center">{title}</h1>
        <p class="text-center">{date} by Tom</p>
            <slot />
      </article>
</MainLayout>